# TUI Package Environment Variables (Optional)
# The TUI uses bundled SQLite databases by default and doesn't require configuration

# Optional: Override database path for testing
# DATABASE_URL="file:./test.db"

# Optional: For testing with PostgreSQL (not typical for TUI)
# DATABASE_URL="postgres://katasumi:dev_password@localhost:5432/katasumi_dev"
# DB_TYPE="postgres"
