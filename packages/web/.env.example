# Web Package Environment Variables
# Copy this file to .env.local and configure for your local environment

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
# For local development with Docker:
DATABASE_URL="postgres://katasumi:dev_password@localhost:5432/katasumi_dev"

# ---------------------------------------------------------------------------
# Node environment
# ---------------------------------------------------------------------------
NODE_ENV="development"

# ---------------------------------------------------------------------------
# NextAuth / JWT
# ---------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# ---------------------------------------------------------------------------
# Base URL (used in Stripe success/cancel redirects)
# ---------------------------------------------------------------------------
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# ---------------------------------------------------------------------------
# Stripe Payment Integration
# ---------------------------------------------------------------------------
# Get your keys at: https://dashboard.stripe.com/apikeys
#
# Test keys start with sk_test_ / pk_test_
# Live keys start with sk_live_ / pk_live_
#
STRIPE_SECRET_KEY="sk_test_..."
# Exposed to the browser (safe — it's a publishable key)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Webhook signing secret — two ways to get it:
#   1. Local testing via Stripe CLI:
#        stripe listen --forward-to localhost:3000/api/stripe/webhook
#      The CLI will print a "whsec_..." secret — paste it here.
#   2. Production: Stripe Dashboard → Developers → Webhooks → your endpoint → Signing secret
STRIPE_WEBHOOK_SECRET="whsec_..."

# Price ID for the subscription plan
# Create a product + recurring price in: https://dashboard.stripe.com/test/products
# Then copy the price ID (starts with price_...)
STRIPE_PRICE_ID="price_..."

# ---------------------------------------------------------------------------
# Optional: AI API keys for AI-powered search features
# ---------------------------------------------------------------------------
# OPENAI_API_KEY="your-openai-key-here"
# ANTHROPIC_API_KEY="your-anthropic-key-here"
